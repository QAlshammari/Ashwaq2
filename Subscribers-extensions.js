function renderExtendNames(){const box=$('extendNames');box.innerHTML=members.map(m=>`<label><input type="checkbox" value="${m.id}" ${selected.has(String(m.id))?'checked':''}><span>${esc(m.name)}<small>${esc(m.username)}</small></span></label>`).join('')||'<span>لا يوجد مشتركون</span>'}
document.querySelectorAll('input[name="extendScope"]').forEach(r=>r.addEventListener('change',()=>{const specific=document.querySelector('input[name="extendScope"]:checked').value==='selected';$('extendNames').hidden=!specific;if(specific)renderExtendNames()}));
$('extendNames').addEventListener('change',e=>{if(!e.target.matches('input[type="checkbox"]'))return;e.target.checked?selected.add(String(e.target.value)):selected.delete(String(e.target.value));render()});
$('extendDays').addEventListener('click',e=>{const btn=e.target.closest('[data-days]');if(!btn)return;const days=Number(btn.dataset.days),scope=document.querySelector('input[name="extendScope"]:checked').value,ids=scope==='all'?new Set(members.map(m=>String(m.id))):selected;if(!members.length)return toast('لا يوجد مشتركون');if(!ids.size)return toast('حددي مشتركًا واحدًا على الأقل');if(scope==='all'&&!confirm(`تأكيد تمديد جميع المشتركين ${days} يوم؟`))return;members=members.map(m=>ids.has(String(m.id))?{...m,start:iso(new Date(new Date(m.start+'T12:00:00').getTime()+days*86400000))}:m);localWrite();render();renderExtendNames();toast(`تم تمديد ${ids.size} مشترك لمدة ${days} يوم`)});
renderExtendNames();
